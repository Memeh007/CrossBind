# DESIGN note — predictive instrument surfaces (no redesign)

**Status:** Documentation only. **Do not implement UI chrome** until Alexander says go.  
**Authority:** Append conceptually to `docs/DESIGN.expanded.md` § components / data density.

When UI is eventually authorized, the predictive pre–in-vivo stack needs these **instrument surfaces** (content contracts, not visual restyles):

1. **Method-tagged metric cards** — every score shows engine/field name + honesty hint (`vina_affinity`, `gnina_cnn_*`, `posebusters_pass`, `admet_ai`, …).
2. **Validity chip** — PoseBusters pass/fail before contact proof is treated as trustworthy.
3. **Evidence pack download** — JSON/MD/TSV as a first-class job action (study desk without new IA).
4. **Ortholog matrix** — species × method × identity with PlanMine RBH banner distinct from Alliance.
5. **Engine health pills** — already present; extend for PoseBusters/ADMET-AI/DiffDock availability.
6. **Density dial stays 8** — tables and packs are cockpit data, not marketing.

Anti-slop unchanged: no purple SaaS, no fake KD animations, no decorative particles.

*Added 2026-09-22 PT for MiniCPM backbone coherence.*
